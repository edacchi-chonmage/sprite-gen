# 認証情報と制作データの扱い

このアプリは、Tailscale内など信頼できる接続元から使う個人向けの制作環境。ユーザー別ログインは実装していないため、インターネット全体へ直接公開しない。

## 認証と保存

- OpenAIのAPIキーは環境変数または `.env.local` に置く。Gitへ含めない。
- Claudeは既存のサブスクログインを利用する。画像APIのキーをClaudeの子プロセスやプロンプトへ渡さない。
- 作品・会話・生成報告は `data/` に保存する。バックアップにも制作内容が含まれるため、コードと分けて管理する。
- 画面からの書き込みは接続元とJSON形式を検査する。静的配信は画像に限定し、設定や生成報告のJSONは公開しない。

## 依存関係と検査

実行時のPython依存はPillow、NumPy、python-dotenv。範囲は `pyproject.toml` を正とする。画像生成はOpenAI API、会話処理はOrcaとClaude CLIを使う。

依存の更新時には脆弱性情報を確認し、宣言したPython環境でテストする。NumPyを欠いた場合に別実装へ切り替えず、明示的に失敗する。

`.gitleaks.toml` は秘密情報検査の設定。任意のローカル検査用 `.githooks/pre-commit` を利用するには、別途 `safedeps` とGitのhooksPath設定が必要。ファイルが存在するだけで検査が有効になっているとは扱わない。実際に実行していない検査を合格済みと報告しない。
